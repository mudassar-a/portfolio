# mudassar.tech — personal academic portfolio

Static single-page site (plain HTML/CSS, no build step). Served via GitHub Pages with the custom domain `mudassar.tech` (see `CNAME`).

To update: edit `index.html`, commit, push.
